<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/">Posts</a>
        </li>
        <li class="nav-item">
          <%= link_to "Users", users_path(:format => "js"), method: :get, :remote=> true, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Saved", saved_posts_path(:format => "js"), :remote => true, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Profile", users_path(id: session[:user_id], :format => "js"),:remote => true, class: "nav-link" %>
        </li>
      </ul>
      <ul class="logins">
      	<% if session[:user_id].present? %>
      		<%= link_to "Logout", session_path(session[:user_id]),  data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }%>
      	<% else %>
      		<%= link_to "Login",root_path, class: "nav-link" %>/
      		<%= link_to "signup",new_user_path(:format => "js"), :remote=> true, class: "nav-link" %>
      	<%end %>
      </ul>
    </div>
  </div>
</nav>